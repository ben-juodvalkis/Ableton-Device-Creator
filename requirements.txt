# Ableton Device Creator V2.0 Requirements

# Core dependencies
# No external Python dependencies required!
# All utilities use Python standard library only

# Optional dependencies for development
# pytest>=7.0.0  # For running tests (when implemented)
# black>=22.0.0  # For code formatting
# flake8>=4.0.0  # For linting

# Note: This project is intentionally dependency-free for maximum portability
# All ADG/ADV manipulation uses:
# - gzip (standard library)
# - xml.etree.ElementTree (standard library)
# - pathlib (standard library)
# - os, sys, shutil (standard library)
